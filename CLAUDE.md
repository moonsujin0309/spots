# 스팟스 — 주말 데이트·나들이 루트 (모바일 웹)

장소 하나가 아니라 **하루의 순서**를 파는 서비스. 지도앱은 "이 가게"를 보여주고,
여기는 "이 가게를 낀 하루"를 보여준다.

**이 작업공간의 다른 프로젝트(굿슬립 — 수면 앱)와 무관한 별개 프로젝트다.**
굿슬립의 CLAUDE.md 규칙은 여기 적용되지 않는다.

**이어서 작업한다면 `docs/다음-작업.md` 부터 읽을 것.**
지금 상태 · 다음에 할 일 · 검증 환경 만드는 법 · 이미 걸렸던 함정이 거기 있다.

문서 안내: `docs/README.md`

## 지금 어디까지 왔나 (2026-09-01)

화면은 원래 완성돼 있었고, 서버 연동 코드가 다 들어갔다. **계정만 붙이면 동작한다.**
`SB_URL`·`SB_ANON` 두 줄이 비어 있어 지금은 로컬 전용으로 돈다.

| 됨 | 안 됨 |
|---|---|
| 스키마 · RLS · 시드 이전 도구 | Supabase 프로젝트 (사람이 만들어야) |
| 카카오 로그인 · 루트 게시 · 저장/좋아요/팔로우 서버 이전 | 카카오 콘솔 설정 (사람이) |
| 사진 업로드(1080px 리사이즈) · 제보 · 신고 · 차단 · 회원 탈퇴 | 관리자 화면 (지금은 `운영-쿼리.sql` 로 대체) |
| 약관 · 처리방침 · 작성자 지침서 초안 | 법률 검토 · 도메인 · 알바 계약 서명 |

## 이 앱이 파는 것

"실행 가능성을 검증한 루트" 하나다. 다른 데이트 앱과 다른 점은 **직접 가본 사람이 쓴다**는 것뿐이다.

기능을 추가할 때 이 질문을 먼저 통과시킬 것 — *이게 "가봤다"를 더 믿게 만드는가?*

검색으로 쓸 수 있는 문장은 이 서비스에 필요가 없다. 이미 인터넷에 널려 있고,
사용자는 그걸 못 믿어서 여기 온 것이다.

## 건드리기 전에 알아야 할 결정들

이유 없이 뒤집지 말 것. 전부 근거가 있다.

| 결정 | 이유 |
|---|---|
| **단일 `index.html`.** 빌드 없음 | 파일 하나면 배포가 push 하나로 끝난다. Tailwind·shadcn 은 프레임워크가 아니라 **구조만** 순수 CSS 로 가져왔다(radius 하나에서 파생되는 스케일, 시맨틱 토큰 이름) |
| 지도는 **카카오 SDK**. 실패하면 SVG 개요도로 폴백 | 가게 이름 라벨이 뜨는 게 목적이다. 카카오는 Leaflet 의 zoom 대신 **level** 을 쓴다 — 숫자가 클수록 넓게 보인다(반대) |
| 지도 **JS 키는 페이지에 노출돼도 된다.** REST 키는 절대 금지 | JS 키는 콘솔에 등록한 도메인에서만 동작한다. 공개 저장소라 REST 키는 넣는 즉시 노출된다 |
| 도보 경로는 **Valhalla**(FOSSGIS 공개 서버). OSRM 아님 | OSRM 공개 데모는 프로파일이 driving 하나뿐이라 걷는 구간까지 차도로 그린다. 실측 서울숲→성수 — OSRM 2,603m / Valhalla 보행 2,061m. 우리 추정(직선×1.3 = 2,075m)과 맞는 건 후자다 |
| 경로는 **그림에만 쓴다.** 시간·거리·비용은 직선×1.3 추정 그대로 | 네트워크 응답에 따라 화면 숫자가 달라지면 같은 루트가 볼 때마다 다르게 보인다 |
| **구간 거리가 이동수단을 정한다**(`leg()`) — 1.2km↓ 도보 / 3km↓ 버스 / 7km↓ 지하철 / 그 이상 혼합 | 이 서비스는 걷는 루트만이 아니다. 실측 구간의 46%가 도보 범위를 넘는다. "도보 몇 분"으로 기준을 세우면 정상 루트의 절반이 걸린다 |
| 장소 검색은 **카카오 services.Places → Photon(OSM) 폴백** | 검색은 막히면 안 되는 흐름이다. 키가 없거나 SDK 가 안 뜨면 조용히 넘어간다 |
| 공유는 **텍스트 한 덩어리**. 카톡 SDK 카드 아님 | 카드는 text 가 190자 제한이라 스팟 목록이 안 들어가고, 공유 시트로 넘길 때 url 필드를 버리는 앱이 있어 **링크가 빠진 채 나갔다.** 링크를 본문 안에 넣는다 |
| 전역 이름이 다르다 — 지도는 소문자 `kakao`, 공유는 대문자 `Kakao` | 도메인 등록 자리도 다르다. 지도는 '플랫폼 키 > JavaScript SDK 도메인', 공유는 '제품 링크 관리 > 웹 도메인'. 후자를 빠뜨리면 공유만 조용히 실패한다 |
| localStorage 에 **좌표·주소를 넣지 않는다.** 장소 키만 | 다녀온 표시는 지도에 안 그리므로 키만으로 충분하다 |
| **개인위치정보를 서버에 저장하지 않는다** | 저장하는 순간 위치정보법상 위치기반서비스사업 신고 대상이 된다. `visits` 에도 장소 키만 보내고 좌표·시각은 안 보낸다. 기능 추가 전에 반드시 검토할 것 |
| 활동 점수는 **저장하지 않고 매번 계산**하며 숫자로 노출하지 않는다 | 남은 만큼을 '할 일'로 바꿔 보여준다 |
| 입력 글자는 **16px 이상**(현재 17px) | iOS 사파리는 16px 미만이면 포커스할 때 화면을 확대해버린다 |
| 터치 타겟 44px 보정 | 2026-08-18 UI/UX 감사 |
| 버튼 `:active` 는 `scale(.98)` | shadcn 은 눌러도 크기가 안 변하지만 모바일에선 촉각 피드백이 사라지면 반응이 없어 보인다. 의도적인 이탈 |
| 사진은 **위키미디어 공용** 출처. 저장소에는 안 올린다 | `.gitignore` 가 전부 막는다. `tools/photo-extract.js` 로 빼서 CDN 에 올린다 |

## 서버 (Supabase) — 2026-09 추가

| 결정 | 이유 |
|---|---|
| `SB_URL`·`SB_ANON` **두 줄이 비면 아무 요청도 안 보내고 예전처럼 동작한다** | 카카오 키가 없을 때 Photon 으로 넘어가는 것과 같은 방식. 서버가 없어도 앱은 돈다 |
| 서버 루트는 **시드 136개를 대체하지 않고 앞에 얹는다** | 서버가 죽어도 화면이 비지 않는다 |
| **places 를 routes 보다 먼저 읽는다.** 장소를 못 찾는 루트는 건너뛴다 | 앱 곳곳이 `PLACES[key]` 를 참조한다. 그리다가 깨지느니 안 보이는 편이 낫다 |
| 쓰기는 **화면 먼저, 서버 나중** | 서버를 기다리면 버튼이 굼떠 보인다. 실패하면 상태를 되돌리고 토스트로 알린다 |
| 읽기는 로그인 없이. **저장·좋아요·만들기에서만** 로그인을 요구한다 | 처음 온 사람에게 로그인 벽을 세우지 않는다 |
| 탈퇴는 **DB 함수 `delete_my_account()` 하나로만** 연다 | 클라이언트가 직접 delete 를 날리면 RLS 로 막을 수 없는 구멍이 생긴다 |
| 탈퇴 동작은 삭제 순서가 아니라 **외래키의 `on delete` 규칙**이 정한다 | `auth.users` 한 줄을 지우면 profiles 가 cascade 로 지워지고 나머지가 규칙대로 퍼진다. 규칙을 안 걸면 "routes 가 참조 중"이라며 삭제가 통째로 막힌다 — 실제로 그랬다 |
| 사진 버킷은 **public** | 비공개면 볼 때마다 서명 URL 왕복이 붙는다. 목록 화면에 사진 수십 장이면 그만큼이다. 공개 게시물에 붙는 사진이라 숨길 것이 없다 |
| 사진은 **브라우저에서 긴 변 1080px 로 줄여** 올린다 | 원본 4MB 가 그대로 들어오면 목록 화면이 죽는다. 실측 2000×1500 → 1080×810 · 6KB |
| **제보와 신고는 다른 테이블** (`place_reports` / `content_reports`) | 앞은 데이터 품질, 뒤는 법적 의무(정보통신망법 임시조치). 섞으면 "폐업했어요"와 "욕설이에요"가 한 큐에 들어와 둘 다 늦어진다 |
| **차단은 신고와 다르다.** RLS 로 막고 로컬 목록에서도 즉시 걷어낸다 | 신고는 운영자 판단까지 시간이 걸리고 모두에게 적용되지만, 차단은 내가 지금 정하고 나에게만 적용된다. 이미 받아 온 것을 안 걷어내면 "차단했는데 그대로네"가 된다 |
| Supabase 리전은 **서울(ap-northeast-2)** | 다른 리전이면 개인정보 국외 이전 고지 의무가 생긴다. **생성 후 변경 불가** |
| `service_role` 키는 **앱에 절대 넣지 않는다** | 공개 저장소다. 관리자 기능이 필요하면 Edge Function 뒤에 두고 `role='admin'` 을 검사한다 |

## 콘텐츠 기준

수치는 임의로 정하지 않는다. **기존 루트 136개의 실측 분포에서 뽑았다.**

| 항목 | 기준 | 실측 |
|---|---|---|
| 스팟 수 | 4~5개 | 4개 120건 · 5개 16건 |
| 총 체류 | 200~360분 | 중앙 280분 (215~430) |
| 구간 거리 | 7km 이내 | 중앙 1.0km · 90% 3.5km |
| 코멘트 | 20~80자, 상투어 금지 | 중앙 38자 |
| 제목 | 앱 입력이 25자 상한 (`TITLE_MAX`) | 12~29자 |

코멘트 판별법 하나 — *"이 문장을 다른 카페에 붙여도 말이 되는가?"* 말이 되면 반려다.

기준을 바꾸면 `tools/route-import.js` 의 상수와 `docs/루트-작성-지침서.md` 를 **같이** 고친다.

## 구조

```
index.html          앱 전부 (5,800줄). 데이터·CSS·JS 가 한 파일에
.gitignore          ⚠️ '*' 로 전부 막고 예외만 연다. 새 파일은 여기 추가해야 커밋된다
docs/
  README.md         문서·도구 안내
  schema.sql        Supabase 스키마 (테이블 16개 + RLS + 탈퇴 함수)
  storage.sql       사진 버킷·정책. schema.sql 다음에 한 번 실행
  운영-쿼리.sql      제보 큐·검증 만료·신고 처리·현황
  루트-작성-지침서.md  작성자용
  route-template.csv  제출 양식
  기존루트-개선대상.md  자동 검수로 걸러낸 81건
  이용약관-초안.md · 개인정보처리방침-초안.md
tools/
  seed-export.js    하드코딩 시드 → INSERT 문
  route-import.js   제출 CSV 검수 → SQL
  photo-extract.js  base64 사진 → 파일 (2.4MB → 448KB)
```

`index.html` 안의 주요 구간은 `═════` 주석으로 나뉜다 — 데이터 · 계산 · 지도 · 상태 · 각 화면 · 서버 · 렌더.

## 규칙

- **공개 저장소다.** 기획·리서치·사업 문서는 로컬에만 둔다. `.gitignore` 의 예외 목록이 그 정책이다
- 데이터(장소·루트)는 `index.html` 이 진실 소스다. 생성된 `seed.sql` 을 손으로 고치지 말고
  `index.html` 을 고치고 스크립트를 다시 돌린다
- UI 문구는 짧은 평서문 존댓말("~해요"). 시적인 문장·감탄사·이모지를 쓰지 않는다
- 단정하지 않는다 — "제일 맛있어요"(×) / "9시에 열어요"(○). 사실만 쓴다
- 색은 라임 `#d4ff00`(채우기) + 핫핑크 `#ff007a`. **라임은 밝은 바탕에서 글자색으로 쓰면 안 보인다** —
  채우기는 `--accent`, 글자는 `--accent-tx` 로 토큰이 나뉘어 있다
- 문법 검증: `<script>` 본문을 뽑아 `node --check`. 브라우저 확인은 `python -m http.server` + Playwright

```bash
cd /home/user/spots && python3 -m http.server 8123
```

## 배포

https://moonsujin0309.github.io/spots/ (public 저장소, GitHub Pages)
